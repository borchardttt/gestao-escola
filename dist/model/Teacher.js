import Person from "./Person";
export class Teacher extends Person {
    constructor(id, name, subject, status) {
        super(id, name);
        this.subject = subject;
        this.status = status;
    }
}
//# sourceMappingURL=Teacher.js.map